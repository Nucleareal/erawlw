@DAY_INCREMENT_EVENT

REPEAT CHARANUM
	LOCAL:0 = CHARACTER_RUT_TYPE(COUNT)

	SIF (TALENT:COUNT:122 == 0 && MENSTRUATION_AVAILABLE && LOCAL:0 == 0)
		CFLAG:COUNT:505 = CFLAG:COUNT:505 + 1
	
	IF (LOCAL:0 && RUT_AVAILABLE)
		CFLAG:COUNT:505 = CFLAG:COUNT:505 + 1
		LOCAL:1 = RUT_CHECK(COUNT, 1)
	ENDIF
REND

@EJAC_IN_TARGET_VAGINA

;”r—‘—U”­
IF RUT_AVAILABLE && CHARACTER_RUT_TYPE(TARGET)
	IF CFLAG:TARGET:506 < 1
		CFLAG:TARGET:506 = 1
		LOCAL:1 = RUT_CHECK(TARGET, 0)
	ENDIF
ENDIF

VARSET LOCAL , 0

;”DP”»’è
SIF MENSTRUATION_AVAILABLE && MENSTRUATION_CHECK(TARGET) < 0
	LOCAL = 1

SIF RUT_AVAILABLE && (RUT_CHECK(TARGET, 0) >= 3)
	LOCAL = 3
	
IF RAND(5) < LOCAL
	;”DP(ŽÀ‘•—\’è‚È‚µ)
ENDIF
