;ARG->実行する番号(-1でチェックのみ, -2で表示あり), ARG:1->チェックする奴隷

;======================v ここ
@CHECK_ABLUP_AVAILABLE_7, ARG, ARG:1

IF ARG:1 == 0
	ARG:1 = TARGET
ENDIF

VARSET LOCAL, 0

;TARGETを避難
LOCAL:0 = TARGET
TARGET = ARG:1

;既にLv5
;======v ここ
IF ABL:7 >= 5
	SIF ARG == -2
		PRINTW 既にMAXです
	RETURN 0
ENDIF

;LOCAL:1 条件1
;LOCAL:2
;LOCAL:3
;LOCAL:4
;LOCAL:5

;結果
;LOCAL:998

;臆病をみて、あるなら1
LOCAL:100 = TALENT:10

;==============v ここ
SELECTCASE ABL:7
	CASE 0
		LOCAL:50 = 100
	CASE 1
		LOCAL:50 = 1000
	CASE 2
		LOCAL:50 = 5000
	CASE 3
		LOCAL:50 = 15000
	CASE 4
		LOCAL:50 = 35000
ENDSELECT

IF TALENT:27
;==================v ここ
	SELECTCASE ABL:7
		CASE 3
			TIMES LOCAL:50 , 2.00
		CASE 4
			TIMES LOCAL:50 , 3.00
	ENDSELECT
ENDIF

IF TALENT:80
	TIMES LOCAL:50, 0.75
ENDIF

IF TALENT:28
	TIMES LOCAL:50, 0.50
ENDIF

IF ABL:1 < ABL:7 + 1
	SIF ARG == -2
		PRINTFORML %ABLNAME:1%が{ABL:7+1}Lv以上、かつ
	LOCAL:1 |= 4
	LOCAL:2 |= 4
	LOCAL:3 |= 4
ENDIF

;Lv3 -> Lv4
IF ABL:7 == 3 
	;[目立ちたがり]なら無視
	IF TALENT:28 == 0
		SIF ARG == -2
			PRINTFORML %EXPNAME:50%あり、かつ
		IF EXP:50 == 0
			LOCAL:1 |= 2
			LOCAL:2 |= 2
			LOCAL:3 |= 2
		ENDIF
	ENDIF
ENDIF
;Lv4 -> Lv5
IF ABL:7 == 4 
	;[目立ちたがり]なら無視
	IF TALENT:28 == 0
		SIF ARG == -2
			PRINTFORML %EXPNAME:50%2以上、かつ
		IF EXP:50 < 2
			LOCAL:1 |= 2
			LOCAL:2 |= 2
			LOCAL:3 |= 2
		ENDIF
	ENDIF
ENDIF

;恥情の珠で上げる
SIF LOCAL:50 > 0 && JUEL:8 < LOCAL:50
	LOCAL:1 |= 1
	
IF ABL:7 < 2
	SIF EXP:2 == 0
		LOCAL:1 |= 2
ELSE
	SIF EXP:11 == 0
		LOCAL:1 |= 2
ENDIF

;チェックのみなら返却
IF ARG == -1
	TARGET = LOCAL:0
	
	SIF LOCAL:1 == 0 && LOCAL:50 > 0
		RETURN 1
		
	RETURN 0
ENDIF

IF ARG == -2
	;表記1
	IF LOCAL:50 > 0
		CALL PRINT_UPGRADE_AVAILABLE_PREFIX, LOCAL:1, 0
		CALL PRINT_UPGRADE_AVAILABLE_JUEL, 8, LOCAL:50
		IF ABL:7 < 2
			CALL PRINT_UPGRADE_AVAILABLE_EXP, 2, 1
		ELSE
			CALL PRINT_UPGRADE_AVAILABLE_EXP, 11, 1
		ENDIF
		CALL PRINT_UPGRADE_AVAILABLE_STATUS, LOCAL:1
		CALL PRINT_UPGRADE_AVAILABLE_POSTFIX, LOCAL:1
	ENDIF
	
	PRINTL [10] - やめる
ENDIF

INPUT
SIF RESULT == 10
	RETURN 0

IF RESULT < 0 || RESULT > 0
	RESTART
ENDIF

IF (LOCAL:1 && RESULT == 0)
	PRINTL 条件を満たしていません
	RESTART
ENDIF

;===v ここ
ABL:7 += 1

;===============================v ここ===v ここ
PRINTFORMW %CALLNAME%の%ABLNAME:7%が{ABL:7}になりました

SELECTCASE RESULT
	CASE 0
;============v ここ
		JUEL:8 -= LOCAL:50
ENDSELECT

TARGET = LOCAL:0

;=========================================v ここ=v ここ
CALL DISPLAY_KOJO_MESSAGE_PALAMCNG_SLAVE, 7, ABL:7
CALL DISPLAY_KOJO_MESSAGE_PALAMCNG_ASSI , 7, ABL:7
