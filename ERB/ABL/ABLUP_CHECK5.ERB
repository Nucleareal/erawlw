;ARG->実行する番号(-1でチェックのみ, -2で表示あり), ARG:1->チェックする奴隷

;======================v ここ
@CHECK_ABLUP_AVAILABLE_5, ARG, ARG:1

IF ARG:1 == 0
	ARG:1 = TARGET
ENDIF

VARSET LOCAL, 0

;TARGETを避難
LOCAL:0 = TARGET
TARGET = ARG:1

;既にLv5
;======v ここ
IF ABL:5 >= 5
	SIF ARG == -2
		PRINTW 既にMAXです
	RETURN 0
ENDIF

;LOCAL:1 条件1
;LOCAL:2
;LOCAL:3
;LOCAL:4

;結果
;LOCAL:998

;臆病をみて、あるなら1
LOCAL:100 = TALENT:10

;==============v ここ
SELECTCASE ABL:5
	CASE 0
		LOCAL:50 = 1
		LOCAL:51 = 2
	CASE 1
		LOCAL:50 = 50
		LOCAL:51 = 10
	CASE 2
		LOCAL:50 = 600
		LOCAL:51 = 30
	CASE 3
		LOCAL:50 = 7000
		LOCAL:51 = 80
	CASE 4
		LOCAL:50 = 45000
		LOCAL:51 = 150
ENDSELECT

IF TALENT:27
;==================v ここ
	SELECTCASE ABL:5
		CASE 3
			TIMES LOCAL:50 , 2.00
			TIMES LOCAL:51 , 2.00
		CASE 4
			TIMES LOCAL:50 , 3.00
			TIMES LOCAL:51 , 3.00
	ENDSELECT
ENDIF

;快Ａの珠で上げる
SIF LOCAL:50 > 0 && JUEL:2 < LOCAL:50
	LOCAL:1 |= 1
	
SIF LOCAL:51 > 0 && EXP:1 < LOCAL:51
	LOCAL:1 |= 2

;チェックのみなら返却
IF ARG == -1
	TARGET = LOCAL:0
	
	SIF LOCAL:1 == 0
		RETURN 1
		
	RETURN 0
ENDIF

IF ARG == -2
	;表記1
	
	IF LOCAL:50 > 0
		CALL PRINT_UPGRADE_AVAILABLE_PREFIX, LOCAL:1, 0
		CALL PRINT_UPGRADE_AVAILABLE_JUEL, 2, LOCAL:50
		CALL PRINT_UPGRADE_AVAILABLE_EXP, 1, LOCAL:51
		CALL PRINT_UPGRADE_AVAILABLE_STATUS, LOCAL:1
		CALL PRINT_UPGRADE_AVAILABLE_POSTFIX, LOCAL:1
	ENDIF
	
	PRINTL [10] - やめる
ENDIF

INPUT
SIF RESULT == 10
	RETURN 0

IF RESULT < 0 || RESULT > 0
	RESTART
ENDIF

IF (LOCAL:1 && RESULT == 0)
	PRINTL 条件を満たしていません
	RESTART
ENDIF

;===v ここ
ABL:5 += 1

;===============================v ここ===v ここ
PRINTFORMW %CALLNAME%の%ABLNAME:5%が{ABL:5}になりました

SELECTCASE RESULT
	CASE 0
;============v ここ
		JUEL:2 -= LOCAL:50
ENDSELECT

TARGET = LOCAL:0

;=========================================v ここ=v ここ
CALL DISPLAY_KOJO_MESSAGE_PALAMCNG_SLAVE, 5, ABL:5
CALL DISPLAY_KOJO_MESSAGE_PALAMCNG_ASSI , 5, ABL:5
