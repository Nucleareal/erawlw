;ARG->実行する番号(-1でチェックのみ, -2で表示あり), ARG:1->チェックする奴隷

;======================v ここ
@CHECK_ABLUP_AVAILABLE_6, ARG, ARG:1

IF ARG:1 == 0
	ARG:1 = TARGET
ENDIF

VARSET LOCAL, 0

;TARGETを避難
LOCAL:0 = TARGET
TARGET = ARG:1

;既にLv5
;======v ここ
IF ABL:6 >= 5
	SIF ARG == -2
		PRINTW 既にMAXです
	RETURN 0
ENDIF

;LOCAL:1 条件1
;LOCAL:2
;LOCAL:3
;LOCAL:4
;LOCAL:5

;結果
;LOCAL:998

;臆病をみて、あるなら1
LOCAL:100 = TALENT:10

;==============v ここ
SELECTCASE ABL:6
	CASE 0
		LOCAL:50 = 100
		LOCAL:51 = 20
		LOCAL:52 = 100
		LOCAL:53 = 1
		LOCAL:54 = 1
	CASE 1
		LOCAL:50 = 1200
		LOCAL:51 = 100
		LOCAL:52 = 0
		LOCAL:53 = 1
		LOCAL:54 = 3
	CASE 2
		LOCAL:50 = 5000
		LOCAL:51 = 600
		LOCAL:52 = 0
		LOCAL:53 = 20
		LOCAL:54 = 6
	CASE 3
		LOCAL:50 = 10000
		LOCAL:51 = 2000
		LOCAL:52 = 0
		LOCAL:53 = 20
		LOCAL:54 = 10
	CASE 4
		LOCAL:50 = 30000
		LOCAL:51 = 8000
		LOCAL:52 = 0
		LOCAL:53 = 100
		LOCAL:54 = 20
ENDSELECT

IF TALENT:27
;==================v ここ
	SELECTCASE ABL:6
		CASE 3
			TIMES LOCAL:50 , 2.00
			TIMES LOCAL:51 , 2.00
			TIMES LOCAL:53 , 2.00
		CASE 4
			TIMES LOCAL:50 , 3.00
			TIMES LOCAL:51 , 3.00
			TIMES LOCAL:53 , 3.00
	ENDSELECT
ENDIF

IF TALENT:80
	TIMES LOCAL:50, 0.75
	TIMES LOCAL:51, 0.75
	TIMES LOCAL:52, 0.75
	TIMES LOCAL:53, 0.75
ENDIF

IF ABL:0 < ABL:6 + 1
	SIF ARG == -2
		PRINTFORML %ABLNAME:0%が{ABL:6+1}Lv以上、かつ
	LOCAL:1 |= 4
	LOCAL:2 |= 4
	LOCAL:3 |= 4
ENDIF

;Lv3 -> Lv4
IF ABL:6 == 3 
	;[妄信]なら無視
	IF TALENT:86 == 0
		SIF ARG == -2
			PRINTFORML %EXPNAME:50%あり、かつ
		IF EXP:50 == 0
			LOCAL:1 |= 2
			LOCAL:2 |= 2
			LOCAL:3 |= 2
		ENDIF
	ENDIF
ENDIF
;Lv4 -> Lv5
IF ABL:6 == 4 
	;[妄信]なら無視
	IF TALENT:86 == 0
		SIF ARG == -2
			PRINTFORML %EXPNAME:50%2以上、かつ
		IF EXP:50 < 2
			LOCAL:1 |= 2
			LOCAL:2 |= 2
			LOCAL:3 |= 2
		ENDIF
	ENDIF
ENDIF

;屈服の珠で上げる
SIF LOCAL:50 > 0 && JUEL:6 < LOCAL:50
	LOCAL:1 |= 1
	
SIF LOCAL:50 > 0 && EXP:2 < LOCAL:54
	LOCAL:1 |= 2
	
SIF LOCAL:50 > 0 && EXP:20 < LOCAL:54
	LOCAL:1 |= 2
	
	
;恭順の珠で上げる
SIF LOCAL:51 > 0 && JUEL:4 < LOCAL:51
	LOCAL:2 |= 1
	
SIF LOCAL:51 > 0 && EXP:21 < LOCAL:53
	LOCAL:2 |= 2

;習得の珠で上げる	
SIF LOCAL:52 > 0 && JUEL:7 < LOCAL:50
	LOCAL:3 |= 1
	
SIF LOCAL:52 > 0 && EXP:2 < 1
	LOCAL:3 |= 2

;チェックのみなら返却
IF ARG == -1
	TARGET = LOCAL:0
	
	SIF LOCAL:1 == 0 && LOCAL:50 > 0
		RETURN 1
		
	SIF LOCAL:2 == 0 && LOCAL:51 > 0
		RETURN 1
		
	SIF LOCAL:3 == 0 && LOCAL:52 > 0
		RETURN 1
		
	RETURN 0
ENDIF

IF ARG == -2
	;表記1
	IF LOCAL:50 > 0
		CALL PRINT_UPGRADE_AVAILABLE_PREFIX, LOCAL:1, 0
		CALL PRINT_UPGRADE_AVAILABLE_JUEL, 6, LOCAL:50
		CALL PRINT_UPGRADE_AVAILABLE_EXP, 2, LOCAL:54
		CALL PRINT_UPGRADE_AVAILABLE_EXP, 20, LOCAL:54
		CALL PRINT_UPGRADE_AVAILABLE_STATUS, LOCAL:1
		CALL PRINT_UPGRADE_AVAILABLE_POSTFIX, LOCAL:1
	ENDIF
	
	IF LOCAL:51 > 0
		CALL PRINT_UPGRADE_AVAILABLE_PREFIX, LOCAL:2, 1
		CALL PRINT_UPGRADE_AVAILABLE_JUEL, 4, LOCAL:51
		CALL PRINT_UPGRADE_AVAILABLE_EXP, 21, LOCAL:53
		CALL PRINT_UPGRADE_AVAILABLE_STATUS, LOCAL:2
		CALL PRINT_UPGRADE_AVAILABLE_POSTFIX, LOCAL:2
	ENDIF
	
	IF LOCAL:52 > 0
		CALL PRINT_UPGRADE_AVAILABLE_PREFIX, LOCAL:3, 2
		CALL PRINT_UPGRADE_AVAILABLE_JUEL, 7, LOCAL:50
		CALL PRINT_UPGRADE_AVAILABLE_EXP, 2, 1
		CALL PRINT_UPGRADE_AVAILABLE_STATUS, LOCAL:3
		CALL PRINT_UPGRADE_AVAILABLE_POSTFIX, LOCAL:3
	ENDIF
	
	PRINTL [10] - やめる
ENDIF

INPUT
SIF RESULT == 10
	RETURN 0

IF RESULT < 0 || RESULT >= 3
	RESTART
ENDIF

IF (LOCAL:1 && RESULT == 0) || (LOCAL:2 && RESULT == 1) || (LOCAL:3 && RESULT == 2)
	PRINTL 条件を満たしていません
	RESTART
ENDIF

;===v ここ
ABL:6 += 1

;===============================v ここ===v ここ
PRINTFORMW %CALLNAME%の%ABLNAME:6%が{ABL:6}になりました

SELECTCASE RESULT
	CASE 0
;============v ここ
		JUEL:6 -= LOCAL:50
	CASE 1
;============v ここ
		JUEL:4 -= LOCAL:51
	CASE 2
;============v ここ
		JUEL:7 -= LOCAL:50
ENDSELECT

TARGET = LOCAL:0

;=========================================v ここ=v ここ
CALL DISPLAY_KOJO_MESSAGE_PALAMCNG_SLAVE, 6, ABL:6
CALL DISPLAY_KOJO_MESSAGE_PALAMCNG_ASSI , 6, ABL:6
