;ARG->実行する番号(-1でチェックのみ, -2で表示あり), ARG:1->チェックする奴隷

;======================v ここ
@CHECK_ABLUP_AVAILABLE_10, ARG, ARG:1

IF ARG:1 == 0
	ARG:1 = TARGET
ENDIF

VARSET LOCAL, 0

;TARGETを避難
LOCAL:0 = TARGET
TARGET = ARG:1

;既にLv5
;======v ここ
IF ABL:10 >= 5
	SIF ARG == -2
		PRINTW 既にMAXです
	RETURN 0
ENDIF

;LOCAL:1 条件1
;LOCAL:2
;LOCAL:3
;LOCAL:4
;LOCAL:5

;結果
;LOCAL:998

;臆病をみて、あるなら1
LOCAL:100 = TALENT:10

;==============v ここ
SELECTCASE ABL:10
	CASE 0
		LOCAL:50 = 3000
		LOCAL:51 = 10000
		LOCAL:52 = 1000
		LOCAL:53 = 100
		LOCAL:54 = 20
	CASE 1
		LOCAL:50 = 6000
		LOCAL:51 = 25000
		LOCAL:52 = 3000
		LOCAL:53 = 250
		LOCAL:54 = 40
	CASE 2
		LOCAL:50 = 12000
		LOCAL:51 = 50000
		LOCAL:52 = 6000
		LOCAL:53 = 500
		LOCAL:54 = 60
	CASE 3
		LOCAL:50 = 20000
		LOCAL:51 = 100000
		LOCAL:52 = 15000
		LOCAL:53 = 1000
		LOCAL:54 = 100
	CASE 4
		LOCAL:50 = 40000
		LOCAL:51 = 300000
		LOCAL:52 = 40000
		LOCAL:53 = 2000
		LOCAL:54 = 160
ENDSELECT

IF TALENT:27
;==================v ここ
	SELECTCASE ABL:10
		CASE 3
			TIMES LOCAL:50 , 2.00
			TIMES LOCAL:51 , 2.00
			TIMES LOCAL:52 , 2.00
			TIMES LOCAL:53 , 2.00
			TIMES LOCAL:54 , 2.00
		CASE 4
			TIMES LOCAL:50 , 3.00
			TIMES LOCAL:51 , 3.00
			TIMES LOCAL:52 , 3.00
			TIMES LOCAL:53 , 3.00
			TIMES LOCAL:54 , 3.00
	ENDSELECT
ENDIF

IF TALENT:60
	TIMES LOCAL:50 , 0.25
	TIMES LOCAL:51 , 0.25
	TIMES LOCAL:52 , 0.25
	TIMES LOCAL:53 , 0.25
ENDIF

IF TALENT:72
	TIMES LOCAL:50 , 0.50
	TIMES LOCAL:51 , 0.50
	TIMES LOCAL:52 , 0.50
	TIMES LOCAL:53 , 0.50
ENDIF

IF TALENT:80
	TIMES LOCAL:50 , 0.75
	TIMES LOCAL:51 , 0.75
	TIMES LOCAL:52 , 0.75
	TIMES LOCAL:53 , 0.75
ENDIF

;みんな自慰中毒になって欲しいので異常経験は必要ない

LOCAL:541 = ABL:7
LOCAL:542 = ABL:10 + 1

;露出癖必要
IF LOCAL:541 < LOCAL:542
	LOCAL:1 |= 4
	LOCAL:2 |= 4
ENDIF

;[自慰しやすい][中毒しやすい]なら露出癖必要なし

IF TALENT:60 || TALENT:72
	LOCAL:1 = 0
	LOCAL:2 = 0
ELSEIF ARG == -2
	PRINTFORML %ABLNAME:7%が{ABL:10+1}Lv以上、かつ
ENDIF

;Ｃ感覚が必要
IF ARG == -2
	PRINTFORML %ABLNAME:0%が{ABL:10+1}Lv以上、かつ
ENDIF

IF ABL:3 < ABL:10 + 1
	LOCAL:1 |= 4
	LOCAL:2 |= 4
ENDIF

IF JUEL:5 < LOCAL:50
	LOCAL:1 |= 1
	LOCAL:2 |= 1
ENDIF
IF JUEL:0 < LOCAL:51
	LOCAL:1 |= 1
	LOCAL:2 |= 1
ENDIF
IF JUEL:8 < LOCAL:52
	LOCAL:1 |= 1
	LOCAL:2 |= 1
ENDIF

SIF EXP:10 < LOCAL:53
	LOCAL:1 |= 2
	
SIF EXP:11 < LOCAL:54
	LOCAL:2 |= 2

;チェックのみなら返却
IF ARG == -1
	TARGET = LOCAL:0
	
	SIF LOCAL:1 == 0 && LOCAL:53 > 0
		RETURN 1
		
	SIF LOCAL:2 == 0 && LOCAL:54 > 0
		RETURN 1
		
	RETURN 0
ENDIF

IF ARG == -2
	;表記1
	IF LOCAL:53 > 0
		CALL PRINT_UPGRADE_AVAILABLE_PREFIX, LOCAL:1, 0
		CALL PRINT_UPGRADE_AVAILABLE_JUEL, 5, LOCAL:50
		CALL PRINT_UPGRADE_AVAILABLE_JUEL, 0, LOCAL:51
		CALL PRINT_UPGRADE_AVAILABLE_JUEL, 8, LOCAL:52
		CALL PRINT_UPGRADE_AVAILABLE_EXP, 10, LOCAL:53
		CALL PRINT_UPGRADE_AVAILABLE_STATUS, LOCAL:1
		CALL PRINT_UPGRADE_AVAILABLE_POSTFIX, LOCAL:1
	ENDIF
	
	IF LOCAL:54 > 0
		CALL PRINT_UPGRADE_AVAILABLE_PREFIX, LOCAL:2, 1
		CALL PRINT_UPGRADE_AVAILABLE_JUEL, 5, LOCAL:50
		CALL PRINT_UPGRADE_AVAILABLE_JUEL, 0, LOCAL:51
		CALL PRINT_UPGRADE_AVAILABLE_JUEL, 8, LOCAL:52
		CALL PRINT_UPGRADE_AVAILABLE_EXP, 11, LOCAL:54
		CALL PRINT_UPGRADE_AVAILABLE_STATUS, LOCAL:2
		CALL PRINT_UPGRADE_AVAILABLE_POSTFIX, LOCAL:2
	ENDIF
	
	PRINTL [10] - やめる
ENDIF

INPUT
SIF RESULT == 10
	RETURN 0

IF RESULT < 0 || RESULT >= 2
	RESTART
ENDIF

IF (LOCAL:1 && RESULT == 0) || (LOCAL:2 && RESULT == 1)
	PRINTL 条件を満たしていません
	RESTART
ENDIF

;====v ここ
ABL:10 += 1

;================================v ここ====v ここ
PRINTFORMW %CALLNAME%の%ABLNAME:10%が{ABL:10}になりました

SELECTCASE RESULT
	CASE 0
;============v ここ
		JUEL:5 -= LOCAL:50
		JUEL:0 -= LOCAL:51
		JUEL:8 -= LOCAL:52
	CASE 1
		JUEL:5 -= LOCAL:50
		JUEL:0 -= LOCAL:51
		JUEL:8 -= LOCAL:52
ENDSELECT

TARGET = LOCAL:0

;==========================================v ここ==v ここ
CALL DISPLAY_KOJO_MESSAGE_PALAMCNG_SLAVE, 10, ABL:10
CALL DISPLAY_KOJO_MESSAGE_PALAMCNG_ASSI , 10, ABL:10
