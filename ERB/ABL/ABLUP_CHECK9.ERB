;ARG->実行する番号(-1でチェックのみ, -2で表示あり), ARG:1->チェックする奴隷

;======================v ここ
@CHECK_ABLUP_AVAILABLE_9, ARG, ARG:1

IF ARG:1 == 0
	ARG:1 = TARGET
ENDIF

VARSET LOCAL, 0

;TARGETを避難
LOCAL:0 = TARGET
TARGET = ARG:1

;既にLv5
;======v ここ
IF ABL:9 >= 5
	SIF ARG == -2
		PRINTW 既にMAXです
	RETURN 0
ENDIF

;LOCAL:1 条件1
;LOCAL:2
;LOCAL:3
;LOCAL:4
;LOCAL:5

;結果
;LOCAL:998

;臆病をみて、あるなら1
LOCAL:100 = TALENT:10

;==============v ここ
SELECTCASE ABL:9
	CASE 0
		LOCAL:50 = 200
		LOCAL:51 = 50
		LOCAL:52 = 0
		LOCAL:53 = 1000
	CASE 1
		LOCAL:50 = 1000
		LOCAL:51 = 200
		LOCAL:52 = 0
		LOCAL:53 = 5000
	CASE 2
		LOCAL:50 = 3000
		LOCAL:51 = 500
		LOCAL:52 = 1000
		LOCAL:53 = 0
	CASE 3
		LOCAL:50 = 8000
		LOCAL:51 = 1000
		LOCAL:52 = 2000
		LOCAL:53 = 0
	CASE 4
		LOCAL:50 = 20000
		LOCAL:51 = 2000
		LOCAL:52 = 5000
		LOCAL:53 = 0
ENDSELECT

IF TALENT:27
;==================v ここ
	SELECTCASE ABL:7
		CASE 3
			TIMES LOCAL:50 , 2.00
			TIMES LOCAL:51 , 2.00
			TIMES LOCAL:52 , 2.00
		CASE 4
			TIMES LOCAL:50 , 3.00
			TIMES LOCAL:51 , 3.00
			TIMES LOCAL:52 , 3.00
	ENDSELECT
ENDIF

IF TALENT:81
	TIMES LOCAL:50 , 0.25
	TIMES LOCAL:51 , 0.25
	TIMES LOCAL:52 , 0.25
	TIMES LOCAL:53 , 0.25
ENDIF

IF TALENT:80
	TIMES LOCAL:50 , 0.75
	TIMES LOCAL:51 , 0.75
	TIMES LOCAL:52 , 0.75
	TIMES LOCAL:53 , 0.75
ENDIF

;Lv3 -> Lv4
IF ABL:9 == 3 
	;[解放]なら無視
	IF TALENT:81 == 0
		SIF ARG == -2
			PRINTFORML %EXPNAME:50%あり、かつ
		IF EXP:50 == 0
			LOCAL:1 |= 2
			LOCAL:2 |= 2
		ENDIF
	ENDIF
ENDIF
;Lv4 -> Lv5
IF ABL:9 == 4 
	;[解放]なら無視
	IF TALENT:81 == 0
		SIF ARG == -2
			PRINTFORML %EXPNAME:50%2以上、かつ
		IF EXP:50 < 2
			LOCAL:1 |= 2
			LOCAL:2 |= 2
		ENDIF
	ENDIF
ENDIF

;苦痛の珠で上げる
SIF JUEL:5 < LOCAL:50
	LOCAL:1 |= 1
;欲情の珠で上げる
SIF JUEL:6 < LOCAL:52
	LOCAL:1 |= 1
;レズ経験
SIF EXP:40 < LOCAL:51
	LOCAL:1 |= 2
	
;苦痛の珠で上げる
SIF JUEL:0 < LOCAL:53
	LOCAL:2 |= 1
;欲情の珠で上げる
SIF EXP:40 < LOCAL:51
	LOCAL:2 |= 2


;チェックのみなら返却
IF ARG == -1
	TARGET = LOCAL:0
	
	SIF LOCAL:1 == 0 && LOCAL:52 > 0
		RETURN 1
		
	SIF LOCAL:2 == 0 && LOCAL:53 > 0
		RETURN 1
		
	RETURN 0
ENDIF

IF ARG == -2
	;表記1
	IF LOCAL:52 > 0
		CALL PRINT_UPGRADE_AVAILABLE_PREFIX, LOCAL:1, 0
		CALL PRINT_UPGRADE_AVAILABLE_JUEL, 5, LOCAL:50
		CALL PRINT_UPGRADE_AVAILABLE_JUEL, 6, LOCAL:52
		CALL PRINT_UPGRADE_AVAILABLE_EXP, 40, LOCAL:51
		CALL PRINT_UPGRADE_AVAILABLE_STATUS, LOCAL:1
		CALL PRINT_UPGRADE_AVAILABLE_POSTFIX, LOCAL:1
	ENDIF
	
	IF LOCAL:53 > 0
		CALL PRINT_UPGRADE_AVAILABLE_PREFIX, LOCAL:2, 1
		CALL PRINT_UPGRADE_AVAILABLE_JUEL, 0, LOCAL:53
		CALL PRINT_UPGRADE_AVAILABLE_EXP, 40, LOCAL:51
		CALL PRINT_UPGRADE_AVAILABLE_STATUS, LOCAL:2
		CALL PRINT_UPGRADE_AVAILABLE_POSTFIX, LOCAL:2
	ENDIF
	
	PRINTL [10] - やめる
ENDIF

INPUT
SIF RESULT == 10
	RETURN 0

IF RESULT < 0 || RESULT >= 2
	RESTART
ENDIF

IF (LOCAL:1 && RESULT == 0) || (LOCAL:2 && RESULT == 1)
	PRINTL 条件を満たしていません
	RESTART
ENDIF

;===v ここ
ABL:9 += 1

;===============================v ここ===v ここ
PRINTFORMW %CALLNAME%の%ABLNAME:9%が{ABL:9}になりました

SELECTCASE RESULT
	CASE 0
;============v ここ
		JUEL:5 -= LOCAL:50
		JUEL:6 -= LOCAL:52
	CASE 1
		JUEL:0 -= LOCAL:53
ENDSELECT

TARGET = LOCAL:0

;=========================================v ここ=v ここ
CALL DISPLAY_KOJO_MESSAGE_PALAMCNG_SLAVE, 9, ABL:9
CALL DISPLAY_KOJO_MESSAGE_PALAMCNG_ASSI , 9, ABL:9
