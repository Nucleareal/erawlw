@CONFIGURATION_ASSIPLAY_CONTINUE
LOCAL = -1

PRINTL 助手で調教を終えたときの設定を変更します。

$INPUT_LOOP_0

PRINT 現在助手で調教を終えたときは、次の調教を

FONTSTYLE 1
LOADGLOBAL
IF GLOBAL:403 & 1
	PRINT 助手で始める
ELSE
	PRINT 主人に戻す
ENDIF
FONTSTYLE 0

PRINTL に設定されています。
DRAWLINE

PRINTFORML [  0] - 助手で始める
PRINTFORML [  1] - 主人に戻す
DRAWLINE
PRINTFORML [100] - 戻る
DRAWLINE

$INPUT_LOOP_1
INPUT
IF RESULT == 100
	RETURN 0
ELSEIF RESULT - (GLOBAL:403 & 1) == 0
	GLOBAL:403 ^= 1
	SAVEGLOBAL
	
	PRINTFORM 設定を
	IF GLOBAL:403 & 1
		PRINT 助手で始める
	ELSE
		PRINT 主人に戻す
	ENDIF
	PRINTFORM に変更しました
ELSE
	CLEARLINE 1
	REUSELASTLINE 正しい値を入力してください
	GOTO INPUT_LOOP_1
ENDIF
PRINTL
DRAWLINE
RETURN 0