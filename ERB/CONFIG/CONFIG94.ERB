@CONFIGURATION_SPLITTER_BODY_MENU
LOCAL = -1

PRINTL 四肢改造の表示設定を変更します。

$INPUT_LOOP_0

PRINT 現在メニューに四肢改造を
FONTSTYLE 1
LOADGLOBAL
PRINTFORM 表示\@GLOBAL:405 & 2 ? する # しない\@
FONTSTYLE 0
PRINTL に設定されています。
DRAWLINE

PRINTFORML [  0] -   表示する
PRINTFORML [  1] - 表示しない
DRAWLINE
PRINTFORML [100] - 戻る
DRAWLINE

$INPUT_LOOP_1
INPUT

IF RESULT == 100
	RETURN 0
ELSE
	RESULT = RESULT * 2
ENDIF

IF RESULT - (GLOBAL:405 & 2) == 0
	GLOBAL:405 ^= 2
	SAVEGLOBAL
	
	PRINTFORM 設定を
	IF GLOBAL:405 & 2
		PRINT 表示する
	ELSE
		PRINT 表示しない
	ENDIF
	PRINTFORM に変更しました
ELSE
	CLEARLINE 1
	REUSELASTLINE 正しい値を入力してください
	GOTO INPUT_LOOP_1
ENDIF
PRINTL
DRAWLINE
RETURN 0