@PRINT_TALENT_LIST, ARG
PRINTL ÅyëféøÅz

REPEAT 999
	IF COUNT == 0 && TALENT:ARG:0
		IF EXP:ARG:0 > 0
			PRINTFORM [çƒê∂%TALENTNAME:COUNT%]
		ELSE
			PRINTFORM [%TALENTNAME:COUNT%]
		ENDIF
		CONTINUE
	ENDIF
	
	SIF TALENT:ARG:COUNT == 0
		CONTINUE
		
	IF ((GLOBAL:405 & 1) == 0) && (160 <= COUNT) && (COUNT < 170)
		CONTINUE
	ENDIF

	SIF COUNT > 200 && COUNT <= 300
		FONTSTYLE 1
		
	STR:3 = [%TALENTNAME:COUNT%]
		
	IF COUNT == 89
		FONTSTYLE 1
		STR:3 = [%TALENTNAME:COUNT%:\@CFLAG:ARG:789 < 0 ? è¡é∏ # %NAME:(CFLAG:ARG:789)%\@]
	ENDIF

	PRINTFORM %STR:3%
	
	FONTSTYLE 0
REND
LINEISEMPTY
SIF RESULT:0
	PRINT ï\é¶Ç≈Ç´ÇÈëféøÇÕÇ†ÇËÇ‹ÇπÇÒ
PRINTL

@PRINT_EXP_LIST, ARG
PRINTL Åyåoå±Åz
REPEAT 100
	SIF EXPNAME:COUNT == ""
		CONTINUE
		
	SIF EXP:ARG:COUNT == 0
		CONTINUE
	
	PRINTFORM %EXPNAME:COUNT%:{EXP:ARG:COUNT}  
REND
LINEISEMPTY
SIF RESULT:0
	PRINT ï\é¶Ç≈Ç´ÇÈåoå±ÇÕÇ†ÇËÇ‹ÇπÇÒ
PRINTL

@PRINT_MARK_LIST, ARG
PRINTL ÅyçèàÛÅz
REPEAT 10
	SIF MARKNAME:COUNT == ""
		CONTINUE
		
	SIF MARK:ARG:COUNT == 0
		CONTINUE
	
	SIF COUNT == 3 && MARK:ARG:3
		FONTSTYLE 1
	
	PRINTFORM %MARKNAME:COUNT%:Lv{MARK:ARG:COUNT}  
	
	FONTSTYLE 0
REND
LINEISEMPTY
SIF RESULT:0
	PRINT ï\é¶Ç≈Ç´ÇÈçèàÛÇÕÇ†ÇËÇ‹ÇπÇÒ
PRINTL

@PRINT_ABL_LIST, ARG
PRINTL Åyî\óÕÅz
REPEAT 50
	SIF ABLNAME:COUNT == ""
		CONTINUE
		
	SIF ABL:ARG:COUNT == 0
		CONTINUE
		
	PRINTFORM %ABLNAME:COUNT%:Lv{ABL:ARG:COUNT}  
REND
LINEISEMPTY
SIF RESULT:0
	PRINT ï\é¶Ç≈Ç´ÇÈî\óÕÇÕÇ†ÇËÇ‹ÇπÇÒ
PRINTL

@PRINT_EXTEND_STATUS, ARG
PRINTL Åyì¡ïMéñçÄÅz

;ê∂óùÉ`ÉFÉbÉJ
IF ITEM:45 && MENSTRUATION_AVAILABLE && CAN_CHARACTER_MENSTRUATION_CHECK(ARG)
	LOCAL:0 = MENSTRUATION_CHECK(ARG)
	
	PRINTFORM ê∂óùé¸ä˙{CFLAG:ARG:506}ì˙ ê∂óù/îróëä˙ä‘{CFLAG:ARG:507}ì˙ 
	
	IF LOCAL
		FONTSTYLE 1
		PRINTFORM \@LOCAL > 0 ? ê∂óùíÜ # îróëíÜ\@ 
		FONTSTYLE 0
		PRINTFORM {M:0}ì˙ñ⁄
	ELSE
		PRINTFORM \@M:1 > 0 ? ê∂óù # îróë\@äJénÇ‹Ç≈Ç†Ç∆{M:0}ì˙
	ENDIF
ENDIF

IF ITEM:46 && RUT_AVAILABLE && RUT_CHECK(ARG, 0)
	LOCAL:0 = RUT_CHECK(ARG, 0)
	
	PRINT ëÃì‡éûåv 
	CALL DISPLAY_CALENDER, CFLAG:ARG:505
	
	IF LOCAL:0 > 0
		SIF LOCAL:0 >= 3
			FONTSTYLE 1
	
		PRINTFORM î≠èÓíÜ(Lv{LOCAL:0}) 
		FONTSTYLE 0
	ENDIF
ENDIF

LINEISEMPTY
SIF RESULT:0
	PRINT ï\é¶Ç≈Ç´ÇÈì¡ïMéñçÄÇÕÇ†ÇËÇ‹ÇπÇÒ
PRINTL



@PRINT_DIFFICULTY_GAUGE, ARG

VARSET LOCAL, 0

CALL CALC_TRAIN_DIFFICULTY_DO, ARG
LOCAL = RESULT

CALL CONVERT_TRAIN_DIFFICULTY, LOCAL
LOCAL:1 = RESULT

CALL PRINT_DIFFICULTY, LOCAL:1

DRAWLINE

RETURN 0



;---------------------------------------------------------------
;
;---------------------------------------------------------------
@PRINT_ITEM_WLW

REPEAT 90
	SIF ITEM:COUNT == 0
		CONTINUE

	PRINTFORM %ITEMNAME:COUNT%({ITEM:COUNT})
REND
PRINTL

RETURN 0


@SHOW_INFO_WLW, ARG
DRAWLINE
CALL LIFE_BAR, ARG
IF TARGET == ARG
	CALL SHOW_JUEL
ELSE
	DRAWLINE
ENDIF
CALL PRINT_DIFFICULTY_GAUGE, ARG
CALL PRINT_TALENT_LIST, ARG
CALL PRINT_MARK_LIST, ARG
CALL PRINT_EXP_LIST, ARG
CALL PRINT_ABL_LIST, ARG
CALL PRINT_EXTEND_STATUS, ARG
DRAWLINE
IF ARG == TARGET
	CALL DISPLAY_KOJO_MESSAGE_IMPRESSION_MAIN_SLAVE
	CALL DISPLAY_KOJO_MESSAGE_IMPRESSION_MAIN_ASSI
ENDIF
WAIT
DRAWLINE
